--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Torch (CPU-only build — must be pinned before sentence-transformers pulls it in,
# otherwise pip resolves the default CUDA build which drags in ~1.5GB of nvidia_*
# packages that a CPU-only host like Render will never use, and blows the RAM budget)
torch==2.3.1

# LangChain + RAG
langchain==0.2.6
langchain-community==0.2.6
langchain-core==0.2.11
langchain-huggingface==0.0.3
langchain-google-genai==1.0.7

# Vector store
chromadb==0.5.3

# Embeddings
sentence-transformers==3.0.1

# Google Gemini
google-generativeai==0.7.1
groq>=1.6.0

# Document processing
pymupdf==1.24.5
python-docx==1.1.2
pytesseract==0.3.10
Pillow==10.4.0

# Database (SQLite locally, PostgreSQL on Render via SQLAlchemy)
sqlalchemy==2.0.31
psycopg2-binary==2.9.9

# Utilities
aiofiles==23.2.1
python-slugify==8.0.4
tiktoken==0.7.0
